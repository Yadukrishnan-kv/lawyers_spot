import { EmailSettingsForm } from '@/components/admin/settings/email-settings-form';

export const metadata = { title: 'Email SMTP | Admin' };

export default function EmailSettingsPage() {
  return <EmailSettingsForm />;
}
