import { PaymentSettingsForm } from '@/components/admin/settings/payment-settings-form';

export const metadata = { title: 'Payment Gateway | Admin' };

export default function PaymentSettingsPage() {
  return <PaymentSettingsForm />;
}
